# path: client/.env.example
# Copy this to client/.env and fill in your values.

# API base URL (no trailing slash).
# Vercel/Netlify deployment: set this to your Render backend URL.
VITE_API_URL=http://localhost:5000
